const getAll = () => {
  return 'all quizzes'
}

const get = (id) => {
  return 'get one quiz'
}

const create = (data) => {
  return 'create quiz with params'
}

const update = (id, data) => {
  return 'update quiz'
}

const destroy = (id) => {
  return 'destroy quiz with id'
}

export { getAll, get, create, update, destroy }
